# Results

The `relcompat3d_geom_reliability/` directory provides a small index over the
canonical experiment outputs. Numerical artifacts remain in their versioned
experiment directories to avoid duplicate copies.
