# Source code

`relcompat3d/` contains training, preprocessing, evaluation, audit, robustness,
and paper-table generation entry points. Paper experiments are run through the
pinned Docker services in `configs/relcompat3d/compose.yaml`.
